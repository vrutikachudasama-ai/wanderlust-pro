/**
 * database.js — MongoDB connection (via Mongoose)
 * Call connectDB() once at startup in server.js when you add a database.
 *
 * Current state: stub — the frontend runs without a database.
 * Uncomment and install mongoose when ready:
 *   npm install mongoose
 */

// const mongoose = require('mongoose');

// async function connectDB() {
//   const uri = process.env.MONGO_URI;
//   if (!uri) throw new Error('MONGO_URI is not set in environment variables.');
//   await mongoose.connect(uri);
//   console.log('✅ MongoDB connected:', mongoose.connection.host);
// }

// module.exports = { connectDB };

module.exports = {};
